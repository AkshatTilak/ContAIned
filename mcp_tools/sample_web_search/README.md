# Sample Web Search MCP Server

A mockable web search tool server providing DuckDuckGo/Wikipedia-style queries for agent workflows.

## Tools Included
- `web_search`: Queries the web or returns simulated top results for a query string.
- `fetch_page`: Retrieves sanitized content from a given URL.

## Running the Server
```bash
python server.py --port 8093
```
