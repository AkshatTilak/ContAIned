# Sample Code Executor MCP Server

A sandboxed Python code execution tool server for code evaluation agents and data processing workflows.

## Tools Included
- `execute_python`: Evaluates Python expressions or script snippets in an isolated global scope.
- `json_transform`: Applies python dictionary transformations to an input payload.

## Running the Server
```bash
python server.py --port 8094
```
